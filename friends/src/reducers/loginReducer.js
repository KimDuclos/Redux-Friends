// import {LOGIN, LOGIN_SUCCESS } from '../actions';

const initialState = {
  loginAttempt: false,
  login: true,
}

export const loginReducer = (state = initialState, action) => {
  switch(action.type) {
    default: 
      return state
  }
}